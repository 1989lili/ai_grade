# 待办事项

> 记录日期：2026-08-21（次日继续处理）

## 已决策不做：onefile 启动提速（2026-09-09 实测结论）

- 单文件版启动 **5.0 秒**，全部耗在"把 233.9MB 归档解压写入 `%TEMP%\_MEIxxxxxx`"，属于 onefile 固有成本；Python 侧已是懒加载（`mss`/`PIL`/`pyautogui`/`numpy`/`cv2` 均在函数内导入）。
- 试过且**实测无效**的两个开关（已在 `build.spec` 留注释，勿再尝试）：
  - `EXE(compress=0)`：PyInstaller 6.20 的 `EXE` **没有** `compress` 参数，会被静默忽略；
  - `upx_exclude` 放过 >5MB 巨型库：体积与启动时间均无变化（开销在归档层 zlib，不在 UPX）。
- 包体大头：`cv2.pyd 82.3MB(35%)`、`onnxruntime 35.2MB`、`numpy.libs openblas 19.5MB`、`rapidocr 模型 15.5MB`、`PIL 12.7MB`（含用不到的 `_avif.pyd 7.5MB`）。
- 真要提速只有两条路：**改回 onedir**（1-2s，但会多出 `_internal` 目录）或 **onedir + 安装包**（交付仍是单个 setup.exe）。当前选择：接受 5s，优先做识别精度与评分质量。

## 问题 1：评分过程信息冗余打印（已修复 2026-09-09）

- **现象**：评分模型返回的 final 结果中包含 `student_answer`、`review_analysis` 两个按小题的数组结构，当前前端"评分过程"面板会打印这些信息。
- **期望（2026-09-09 再次确认，以截图为准）**：结果区保留**三段式**——**【学生作答】**（识别出的手写原文，右侧标注识别来源）+ **【阅卷评析】**（模型逐点批语，右侧标注评分模型）+ **【成绩得分】**；需要去掉的只是"评分过程"里**逐字回显的模型原始 JSON 文本**。

示例返回结构（原始 JSON 不再直接展示，内容拍平进上述三段）：

```json
{
    "student_answer": [
        { "question": "(1)", "answer": "维护政权稳定" },
        { "question": "(2)", "answer": "东周时期" },
        { "question": "(3)", "answer": "魏赵燕" },
        { "question": "(4)", "answer": "大秦朝" }
    ],
    "review_analysis": [
        { "question": "(1)", "analysis": "答案正确，但未提及具体目的" },
        { "question": "(2)", "analysis": "答案正确，但未提及具体时间" },
        { "question": "(3)", "analysis": "答案正确，但未提及具体时间" },
        { "question": "(4)", "analysis": "答案正确，但未提及具体时间" }
    ],
    "score": 8,
    "max_score": 10,
    "reasoning": "学生未能提供完整的时间信息"
}
```

- **涉及文件**：前端评分过程渲染逻辑（`js/main.js`）
- **修复**（2026-09-09 最终版：三段结果直接写进"评分过程"题号下面，无任何步骤提示行）：
  1. `js/main.js`：模型原文/JSON 不再逐字上屏（`token` 事件只更新状态行）；删除全部步骤提示行——`① 识别学生作答…`、`[初步得分] x`、`② 阅卷评析完成`、`③ 成绩得分：x`、`④ 正在回填打分框并提交…`、`④ 已回填打分框并提交 ✓` 一律不输出；
  2. `js/main.js` 新增 `appendStreamResult()`：在 `=== 第 N / M 份 ===` **正下方**依次渲染 **【学生作答】**（`student_answer`）、**【阅卷评析】**（`review_analysis`，缺失回退 `reasoning`）、**【成绩得分】**（**只有分数本身**，不带 `/满分`、不带模型名）；数组/逐题对象由 `flattenContent()` 拍平成多行纯文本，绝不出现 JSON 花括号；
     - 配套：`appendStreamContent()` 改用 `createTextNode` 追加（原 `textContent +=` 会把插入的 HTML 段压成纯文本丢样式）；
     - 底部结果框 `#grading-result` 不再重复展示成功结果，仅在**批改失败**时出现（`showGradingResult` 只保留错误分支）；批改进行中给 `.process-panel` 加 `stream-only` 隐藏结果框，让"评分过程"占满面板；
  3. `backend/grading_prompts.py` + `backend/app.py`：阅卷评析必须**讲道理**——按评分点逐条写"学生答了什么 → 是否符合要点 → 得/扣几分及原因"，最后一句汇总得分（≤150 字）；`LLM_STREAM_MAX_TOKENS` 由 400 放宽到 **1024**（注意：智谱 GLM-4V 系列上限即 1024，设更大会被服务端拒成 HTTP 400）。

## 问题 2：连续自动识别第二份试卷时提示"标记丢失，停止修改"（已修复 2026-09-09）

- **现象**：自动批改进行到第二份试卷识别时，弹出提示"标记丢失，停止修改"。
- **根因**：前端每轮批改前调用 `hide_all_markers()` 隐藏标记避免截到浮窗，批完后再调用 `show_marker_at()` 恢复。但旧实现只是把命令丢进 `queue` 就立即返回（tk 线程 50ms 才轮询一次），且前端不 `await` 恢复调用就直接进入下一轮并读取 `get_all_marker_rects()`。此时标记窗口往往还没真正显示出来，`is_visible()` 仍为 False，后端 `get_all_marker_rects()` 只返回可见标记 → 前端把"尚未恢复"误判为"标记丢失"并停止。首张开始时标记本来就是可见的，所以总是第一张成功、第二张才失败。
- **修复**：
  1. 后端 `backend/launcher.py`：标记 show/hide 改为**同步确认**——命令携带 `threading.Event`，tk 线程执行完（无论成败）都会唤醒调用方；单条命令异常只记录日志，不再拖垮整个标记线程。
  2. 前端 `js/main.js`：恢复标记改为逐个 `await`，确保返回时标记已真正显示，下一轮读取坐标必然可见。
  3. "标记丢失"提示同时打印到**评分过程**面板（原仅显示在进度条上）。
- **涉及文件**：`backend/launcher.py`、`js/main.js`
